while True:
     age= int(input("enter your number:"))
     if age ==100:
         print("program ended")
         break
     if age > 18:
          print("you are allowed to enter")
     else:
          print("get out")
